name: Hello world
on:
 push:
  branches:
  - main
jobs:
  first-hello-world-jobs: 
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: print message
        run: echo "Hello world ga testing"

   
